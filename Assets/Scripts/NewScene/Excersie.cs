using UnityEngine;

public class Excersie : MonoBehaviour
{
    public AudioSetting AudioSetting;
    public DifficultSetting DifficultSetting;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       //Debug.Log(AudioSetting.newSetting.volumn);
       //Debug.Log(DifficultSetting.newSetting.dicultyName);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
